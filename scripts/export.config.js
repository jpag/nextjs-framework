module.exports = {
	siteURL: 'https://www.example.com',
	
	exportFolder: 'static-export',
	
	// for API use we need to query the localhost api
	// this is also the PORT we kill.
	devServerPort: '3000',
}